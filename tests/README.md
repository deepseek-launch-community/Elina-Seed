# Tests

Test suite for Elina OS modules.

## Planned Tests

- `test_memory_loop.py` – Memory storage and recall
- `test_safety_verifier.py` – Threat detection and zone access
- `test_identity_layer.py` – Hardware-rooted identity verification

## Running Tests

Tests will use `pytest`. To run:

```bash
pytest tests/

More details coming soon.
